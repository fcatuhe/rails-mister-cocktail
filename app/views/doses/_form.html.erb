<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association :ingredient, collection: Ingredient.all.order(:name), include_blank: false %>
  <%= f.input :description %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
